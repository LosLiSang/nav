# nav

本地优先的个人导航页。当前版本包含分类切换、紧凑书签卡片、增删改、拖拽排序/移动、多搜索引擎、壁纸与透明度设置、日历和备忘录抽屉。

## 开发

```bash
npm install
npx vite --host 127.0.0.1 --port 5173
```

## 构建

```bash
npm run build
```

数据保存在浏览器的 IndexedDB 中，没有服务端数据库。
