# 设计与交互规范

## 一、弹窗与表单规范

- **高度控制**：弹窗高度不得超过一屏（避免长表单垂直平铺），优先使用 Segmented Tabs（分类选项卡）将功能降维解耦。
- **Header & Footer**：标题栏与操作栏固定在首尾，仅中间 Body 允许滚动；外层容器固定 `overflow-hidden` 保护圆角。
- **滚动条体验**：深色模式下严禁暴露系统默认白色宽轨，必须使用定制的半透明胶囊细滚动条兜底。